# Set DOMAIN in .env, for example:
# DOMAIN=blog.example.com
{$DOMAIN:localhost} {
	encode zstd gzip
	reverse_proxy firefly:80

	header {
		X-Content-Type-Options "nosniff"
		X-Frame-Options "SAMEORIGIN"
		Referrer-Policy "strict-origin-when-cross-origin"
	}
}
